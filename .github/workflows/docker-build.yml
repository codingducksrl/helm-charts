name: Docker build

on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  laravel:
    name: Laravel
    uses: ./.github/workflows/laravel/build-laravel.yml
  wordpress:
    name: Wordpress
    uses: ./.github/workflows/laravel/build-wordpress.yml